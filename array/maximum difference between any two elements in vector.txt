#include <bits/stdc++.h>
using namespace std;


int main(){
    
    vector<int> v={2,3,10,6,4,8,1};
    
    int maxi=0;
    int mini=INT_MAX;
    for(int i=0;i<v.size();i++){
        maxi=max(maxi,v[i]);
        mini=min(mini,v[i]);
    }
    cout<<maxi-mini;
    
}

