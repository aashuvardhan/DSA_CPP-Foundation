Node* middle(Node* head){
    Node* slow=head;
    Node* fast=head;
    
    while(fast!=NULL and (fast->next)!=NULL and fast->next->next!=NULL){
        slow=slow->next;
        fast=fast->next->next;
    }
    return slow;
}
