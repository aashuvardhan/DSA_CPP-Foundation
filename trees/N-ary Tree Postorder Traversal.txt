
httpsleetcode.comproblemsn-ary-tree-preorder-traversaldescription


#include bitsstdc++.h
using namespace std;

struct Node{
    int data;
    vectorNode  children;
    
    Node(int x){
        data=x;
    }
};

void postorder(Node root){
    
    if(root==NULL){
        return;
    }
    for(auto ele root-children){
        postorder(ele);
    }
    coutroot-dataendl;
    
    
}



int main() {
	 your code goes here
	
	Node root = new Node(1);

     Create child nodes
    Node child1=new Node(3);
    Node child2=new Node(2);
    Node child3=new Node(4);
    
    
    root-children.push_back(child1);
    root-children.push_back(child2);
    root-children.push_back(child3);
    
    Node child4=new Node(5);
    Node child5=new Node(6);
    
    root-children[0]-children.push_back(child4);
    root-children[0]-children.push_back(child5);
    
    
	postorder(root);
	
}
