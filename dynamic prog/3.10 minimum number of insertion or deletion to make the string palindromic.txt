#include <bits/stdc++.h>
using namespace std;

string reverse(string str){
    int i=0;
    int j=str.size()-1;
    while(i<j){
        char temp=str[i];
        str[i]=str[j];
        str[j]=temp;
        i++;
        j--;
    }
    return str;
}


int main() {
	// your code goes here
	string str="agbcba";
	string rev=reverse(str);
	
	int n=str.size();
	
	vector<vector<int>> tab(n+1,vector<int>(n+1,-1));
	
	for(int i=0;i<=n;i++){
	    tab[i][0]=0;
	}
	for(int j=0;j<=n;j++){
	    tab[0][j]=0;
	}
	for(int i=1;i<=n;i++){
	    for(int j=1;j<=n;j++){
	        
	        if(str[i-1]==rev[j-1]){
	            tab[i][j]=1+tab[i-1][j-1];
	        }else{
	            tab[i][j]=max(tab[i-1][j],tab[i][j-1]);
	        }
	        
	    }
	}
	//cout<<tab[n][n];
	cout<<(n-tab[n][n]);
	

}



/* Memoisation

#include <bits/stdc++.h>
using namespace std;


string reverse(string str){
    int low=0;
    int high=str.size()-1;
    
    while(low<high){
        char temp=str[low];
        str[low]=str[high];
        str[high]=temp;
        low++;
        high--;
    }
    return str;
    
}


int lcs(string str1,string str2,int m,int n,vector<vector<int>>& tab){
    if(m==0 or n==0){
        return 0;
    }
    if(tab[m][n]!=-1){
        return tab[m][n];
    }
    if(str1[m-1]==str2[n-1]){
        tab[m][n]= 1+ lcs(str1,str2,m-1,n-1,tab);
    }else{
        tab[m][n]=max(lcs(str1,str2,m-1,n,tab),lcs(str1,str2,m,n-1,tab));
    }
    return tab[m][n];
}



int main() {
	// your code goes here
	string str1="agbcba";
	string str2=reverse(str1);
	int n=str1.size();
	
	vector<vector<int>> tab(n+1,vector<int>(n+1,-1));
	
	lcs(str1,str2,n,n,tab);
	
	cout<<n-tab[n][n];
}



*/
